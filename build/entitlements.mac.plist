<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Permet à Node.js et aux modules natifs de s'exécuter -->
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>

  <!-- Permet de charger des bibliothèques non signées (ex: sql.js WASM) -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!-- Connexion sortante (pour les mises à jour electron-updater) -->
  <key>com.apple.security.network.client</key>
  <true/>

  <!-- Lecture/écriture sélective via les dialogues de fichiers
       (NSOpenPanel/NSSavePanel — pas d'accès libre) -->
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>

  <!-- Accès Documents/Downloads (sur demande) -->
  <key>com.apple.security.files.downloads.read-write</key>
  <true/>
</dict>
</plist>
